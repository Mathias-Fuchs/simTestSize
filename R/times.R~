   A <- as.numeric(
        system.time(
            replicate(
                100, 
                predictionRule <- learningAlgorithm(drawFunction(n=g, ...))
            )
        )[3]
    )/100

                                        # B is the drawing and testing time
    predictionRule <- learningAlgorithm(drawFunction(n=g, ...))
    B <- as.numeric(
        system.time(
            replicate(
                100, 
                error <- predictionRule(drawFunction(n=testChunkSize, ...))
            )
        )[3]
    )/100
    
